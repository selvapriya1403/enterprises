import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#851d1dff", 
    },
    secondary: {
      main: "#211c1bff", 
    },
  },
});

export default theme;
